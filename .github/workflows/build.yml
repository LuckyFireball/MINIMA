name: Deploy Live Website

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy-site:
    runs-on: ubuntu-latest
    steps:
      - name: Sync Repository Code
        uses: actions/checkout@v4

      - name: Inject Secret Token Safely
        # We must place the env declaration directly on the step so Python can see it
        env:
          THE_KEY: ${{ secrets.THE_KEY }}
        run: |
          python3 -c "
          import os
          with open('main.py', 'r') as f:
              code = f.read()
          # We read it directly from the local environment variable safely
          code = code.replace('THE_KEY_WILL_BE_HERE', os.environ.get('THE_KEY', ''))
          with open('main.py', 'w') as f:
              f.write(code)
          "

      - name: Setup GitHub Pages Website Environment
        uses: actions/configure-pages@v5

      - name: Upload Page Files
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy Live Link
        uses: actions/deploy-pages@v4
